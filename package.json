{
	"name": "lark-end2end-encrypted",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"preinstall": "cd public && npm install",
		"fe-build": "cd public && npm run build",
		"be-build": "echo 'No need when use wrangler deploy'",
		"build": "npm run fe-build && npm run be-build",
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "vitest",
		"cf-typegen": "wrangler types"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.8.19",
		"@cloudflare/workers-types": "^4.20250617.0",
		"typescript": "^5.5.2",
		"vitest": "~3.2.0",
		"wrangler": "^4.23.0"
	},
	"dependencies": {
		"itty-router": "^5.0.18"
	}
}